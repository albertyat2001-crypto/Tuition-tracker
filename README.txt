TUITIONTRACK V8 — BM MONSTERA ANDROID ICON

Replace ALL files in the GitHub Pages repository with:
- index.html
- manifest.json
- sw.js
- icon-192.png
- icon-512.png
- icon-maskable-512.png

The home-screen icon is now explicitly configured in manifest.json using the BM Monstera logo.
The service worker cache is V8 so the browser has a new cache version.

IMPORTANT:
1. Host the files on HTTPS/GitHub Pages.
2. Open the HTTPS address in Chrome.
3. Tap the visible Install App button.
4. If Chrome does not show the automatic prompt, use Chrome ⋮ -> Install app/Add to Home screen.
5. If the old icon remains, remove the old installed TuitionTrack app and install it again.
6. Back up student/attendance/fee data before clearing site data.

The app's existing features are retained from V7: attendance, class-based fees, individual student fees, arrears, payments, WhatsApp reminders, receipts, reports, settings, and backup/restore.
